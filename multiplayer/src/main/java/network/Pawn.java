package network;

import java.awt.*;
import java.util.ArrayList;

public class Pawn {
    /*int row;
    int col;
    Color color;
    boolean isSafe = true;
    Pawn[][] board;
    boolean inHome = false;

    Pawn(int x, int y, Color color, Pawn[][] board) {
        this.row = x;
        this.col = y;
        this.color = color;
        this.board = board;
    }

    private Pawn[][] invertBoard(Pawn[][] board) {
        Pawn[][] nextTurn = new Pawn[board.length][board[0].length];
        for (int i = 0; i < board.length; i++) {
            for (int j = 0; j < board[0].length; j++) {
                nextTurn[i][j] = board[i][j];
            }
        }return nextTurn;
    }
    public String toString(){
        return "(" + row + ", " + col + isSafe +")";
    }
    public Move[] getMoves() {
        ArrayList<Move> moves = new ArrayList<>();
    }
    */
}
